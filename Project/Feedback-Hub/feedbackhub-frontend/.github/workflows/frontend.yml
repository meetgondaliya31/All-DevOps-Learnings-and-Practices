name: feedbackhub-frontend-workflow

on: 
    push:
        branches: [main]

jobs:
    build:

        runs-on: ubuntu-latest

        steps:
            - name: checkout code
            - uses: actions/checkout@v7.0.1

            - name: install dependencies
            - uses: npm i

            - name: npm build
            - uses: npm run build 

            - name: 
