apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apache-child             # Child app name
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/meetgondaliya31/All-DevOps-Learnings-and-Practices.git # Repo containing child apps(that you have forked and cloned earlier)
    targetRevision: main
    path: K8S/ArgoCD/argocd-demos/declarative_approach/online_shop    # path of Apache app in `argocd-demos` repo
  destination:
    server: https://kubernetes.default.svc # Replace with your Cluster Url, To get run: `argocd cluster list`
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
