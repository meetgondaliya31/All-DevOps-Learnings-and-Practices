apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-volume
  namespace: nginx

spec:

  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage
