
** 29/06/26 **

	-> Intro to devops and cloud, Basic info and overall into of devops

	-> Done research abt Linux architecture, Processes Systemd
	
	-> Learned abt all networking concepts
	
	-> Done practice of Process management, File system and Networking troubleshooting commands
	
	-> Checked running processes, Inspected sysemd service (This is the first process tht run when sys starts, start-stop running proc)
	





** 30/06/26 **

 	-> CUP(top, ps), Memory(free -h), Disk(df -h), networking commands (ss -lntp : shows all addresses with ports and running processes of particular port)
	
	-> To saw manual for particular command
	
	-> Trace logs : /var/log
	
	-> Basic File commands
	
	-> Linux File System Hierarchy (the most important directories)
	
	-> AWS ec2 instance, connect via ssh, Install nginx
	
	-> AWS basic concepts : 
		- Cloud computing
		- ec2 service
		- public, private and hybrid cloud 
		- AWS region, availability zone, Local zone
		- cat /etc/os-release : To check the OS type
		- lscpu : To check the number of cpu
		- Default availability zone is different for all user, like ap-south-1a may be 1b in my acc or 1c
		- cat /etc/os-release : used to display OS info
		- service ngnix status : To check nginx status
		- /var/www/html : path of the localhost server file
		- curl localhost : command to show the localhost page output, command-line tool to send HTTP requests to a web server running on the local machine and allows developers to test and debug web app, APIs and servers locally without needing to deploy code to a live internet server
		
		
		
		
		
		
** 30/06/26 **

	-> Create Security groups 
		- Inbound rule : For incoming data
		- Outbound rule : For outgoing request/data
		- We can add Protocol or port number when creating security group
		- We can also add source and destination IP like : my own IP or IPv4 or IPv6
		
	-> accton : sudo accton [filename], dump-acct [filename] (If we don't give any file name then it will save on /var/log/account/pacct), to turn off(accton off)
	
	-> useradd -m leo : here -m is used to create user with directory
	
	-> Add user to a group : useradd -G (group_name) (user_name) and 2nd command : usermod -aG (group_name) (user_name) [-a refers to append the user to a group, means its gives all permission to user which that group have, like we can append our user to sudo group to access the servicezs of sudo using 'sudo']
	
	-> Set group owner to any group
	
	-> Understand and modify permissions using chmod
	
	-> Set permissions to 775 (rwxrwxr-x) : chmod 775 (dir_name)
		- Test by creating files
		
		
		
		
		
		
** 02/7/26 **

	-> zip, unzip, tar, untar
	
	-> scp (secure copy) 
		- Local to Remote : scp -i "/home/meet/Downloads/first.pem" -r local  ubuntu@ec2-13-127-177-59.ap-south-1.compute.amazonaws.com:/home/ubuntu/
		- Remoe to Local : scp -i "/home/meet/Downloads/first.pem" -r ubuntu@ec2-13-127-177-59.ap-south-1.compute.amazonaws.com:/home/ubuntu/remote .
	
	-> rsync
	
	-> Networking comands...
	
	-> ping : display conection between yr server and given website and display packet transmission
	
	-> netstat : display active internet connection of yr system
	
	-> ifconfig(interface configuration) : display IPv4, IPv6, flags and packet related info of any network of yr system like.. ethernet, localhost, wifi etc
	
	-> traceroute & tracepath : trace and display the route and path of the request sent and received by us to any server 
	
	-> mtr(my trace route) : short-cut command to view ping and route both
	
	-> nslookup : display domain name and IP address of given website
	
	-> telnet : same as nslookup but we have to give check specific port by giving port number at the end of command 
	
	-> hostname : can check hostname
	
	
		
		
			
	
		
		
		
		
		

