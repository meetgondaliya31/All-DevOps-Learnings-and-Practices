** 13/07/26 **

	-> AWS roles : ex. Create a role for any permission like ec2fullaccess then attach this role to any user so they can access all ec2 services without credentials.
	
	-> cloud shell : used for temporary cli use from aws ec2 service
	
	-> access advisor : used to keep track of user's action and used services 
	
	
	
	
	
	
** 14/07/26 **

	-> S3 : create bucket/object
		- public yr s3 bucket object by policy generator
		- versioning
		- host static website
		- redirect in s3 stactic website from one to another
		- change prefix in static website
		- accelerated transfer ( aws s3 ls ,  
			aws s3 cp 11.png s3://accelerated-transfer-testing(s3://<bucket_name> ,  
			aws configure set s3.addressing_style virtual
			aws s3 cp 11.png s3://accelerated-transfer-testing --endpoint-url http://s3-accelerate.amazonaws.com(has to remove somthing))
		- same/cross region replication
		- configure logging in s3 bucket
		
		
		
		
		
		
** 15/07/26 **

	-> s3 :
		- storage class configuration (various model of objects according to price and services)
		- storage class > Create life-cycle rule
		- intelligent tiering archive configurations 
		- CORS
		- presigned URL
		- s3 server side encryption 
		
		
		
		
		
		
** 17/07/26 **

	-> cloudFront : Amazon CloudFront is a web service that speeds up distribution of your static and dynamic web content, such as .html, .css, .js, and image files, to your users. CloudFront delivers your content through a worldwide network of data centers called edge locations. When a user requests content that you're serving with CloudFront, the request is routed to the edge location that provides the lowest latency (time delay), so that content is delivered with the best possible performance.
	
		- invalidation : use to remove data from cloudfront so that we can get latest edited data from server
		
		- ec2 instance accessible from cloudfront :
			- using vpc > managed prefix list > cloudfront > we can get prefix list name which we can add to the security rule so that other user can not access server directly, they can only use it from cloudfront url
			
		- s3 private bucket with cloudfront
		
		- cloudfront path base routing with multiple origin (ec2 + s3)
