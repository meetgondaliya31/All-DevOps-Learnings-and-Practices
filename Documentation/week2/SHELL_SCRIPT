** 07/07/26 **

	-> shebang (#!/bin/bash) : The shebang (#!/bin/bash) is the first line of a script that tells the kernel which interpreter to use for execution.  It matters because it automates script execution, allowing you to run a file directly (e.g., ./script.sh) without manually typing the interpreter command (e.g., bash script.sh).
	
	-> vriable, echo, read
	
	-> if-else, for loop, while loop
	
	
	
	
	
	
** 08/07/26 **

	-> functions :
		- leo() {
		    	  mkdir demo
		  }
		  demo
		- arguments in function
	
	-> error handling : created flask app using error handling
	
	
	
	
	
	
** 09/07/26 **

	-> CLI to AWS (API integration)
		- aws ec2 describe-instance --region ap-south-1 (comand to get mata-data of an instance)

	-> makefile : make (created shortcut key word)
	
	-> back-up rotation (small project )
	
	-> echo $? : takes the output of previous command and give output in 0(successful), 1(error) or any other number(inappropriate command)
