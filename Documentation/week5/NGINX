** 29/07/26 **

	-> Nginx :
	
	-> hosting custom static website 
		- ec2 server's index.html path - /usr/share/nginx/html/
		- local path for nginx index.html file - /var/www/html/
	
	-> nginx.conf file (path - /etc/nginx)
	
	-> MIME types
	
	-> configure error page
		- error_page 404 /404.html;
		  location / {
                  	try_files $uri $uri/ =404;
                }
                
	-> https in nginx (certbot certificates)
	
	-> reverse proxy 
		- proxy_pass url;
	
	-> load balancing 
		- upstream backend_servers {
                  	  server 35.154.27.168:80;
                  	  server 13.235.80.218:80 backup;
          	  }
          
          	  location / {
                          proxy_pass http://backend_servers;
                  }
          
          
















start ec2 
ssh gen key
first - install node in cli
then nginx
copy backend files to nginx directory   git clone backend
pm2 start with name

back-pipeline - git pull-> npm i-> pm2 restart or pm2 start 

nginx revese proxy backend access 

frontend pipeline -
git clone-> npm install-> npm run build with secret variable(backend url)-> copy dist folder from local to server 

front dir & back dir specific repo for both 
