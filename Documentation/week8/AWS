** 18/08/26 **

	-> VPC peering : we can ping both instance from eachother by private ip 
		- use vpc peering and then edit route table's routes (add eachother's public ip)
		
	-> Transit gateway : we can connect multiple instances in a star topology way that any of them can ping each of them
		- then need to setup transit gateway attachments
		
	-> flow logs : creates the log of specific service and store it in s3 bucket, cloud watch etc
	
